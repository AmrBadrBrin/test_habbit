# Установка Cloudflare Tunnel (альтернатива localtunnel)

## Способ 1: Прямая загрузка (рекомендуется)

1. **Скачайте cloudflared для Windows:**
   https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe

2. **Переименуйте файл:**
   - Найдите скачанный `cloudflared-windows-amd64.exe`
   - Переименуйте в `cloudflared.exe`
   - Переместите в удобную папку, например: `C:\cloudflared\`

3. **Добавьте в PATH (опционально):**
   - Откройте Настройки → Система → О системе → Дополнительные параметры системы
   - Переменные среды → PATH → Изменить
   - Добавьте путь к папке с cloudflared (например: `C:\cloudflared\`)

4. **Или запускайте напрямую:**
   ```cmd
   C:\путь\к\cloudflared.exe tunnel --url http://localhost:3000
   ```

## Способ 2: Через winget с явным источником

```cmd
winget install Cloudflare.cloudflared --source winget
```

## Способ 3: Используем ngrok для обоих (самый простой!)

Вместо cloudflared можно использовать ngrok для backend тоже, но с маленькой хитростью.

**Закройте окно localtunnel** и запустите:

```cmd
ngrok http 3000 --host-header=localhost:3000
```

Скопируйте URL для backend.

Потом в другом окне:
```cmd
ngrok http 5173 --host-header=localhost:5173
```

Скопируйте URL для frontend.

**ПРОБЛЕМА:** Бесплатный ngrok позволяет только 1 туннель одновременно.

## Рекомендация: Давайте сразу задеплоим!

Вместо всех этих локальных туннелей, давайте за 5 минут задеплоим:
- **Backend на Render** (бесплатно)
- **Frontend на Vercel** (бесплатно)

Получите постоянные HTTPS URL без паролей и ограничений!

Хотите задеплоить? Это проще чем настраивать туннели! 🚀
