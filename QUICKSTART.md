# Быстрый старт

## 1. Установите ngrok
Скачайте с https://ngrok.com/download и зарегистрируйтесь для получения authtoken.

## 2. Создайте Telegram бота
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. **Bot Token уже добавлен в server/.env**

## 3. Запустите серверы (уже запущены!)

✅ Backend уже запущен на http://localhost:3000
✅ Frontend уже запущен на http://localhost:5173

## 4. Настройте ngrok для двух туннелей

**ВАЖНО**: Бесплатный ngrok позволяет только 1 сессию. Нужно использовать config файл!

### Вариант A: Через ngrok config (рекомендуется)

1. Получите authtoken: https://dashboard.ngrok.com/get-started/your-authtoken
2. Откройте `ngrok-config.yml` и замените `YOUR_AUTHTOKEN_HERE` на ваш токен
3. Запустите: **двойной клик на `start-ngrok.bat`**

Или вручную:
```bash
cd C:\Users\badre\OneDrive\Рабочий стол\mini_app_new
ngrok start --all --config ngrok-config.yml
```

### Вариант B: Альтернатива без config

Используйте другой сервис для одного туннеля:

**Backend через localtunnel:**
```bash
npx localtunnel --port 3000
```

**Frontend через ngrok:**
```bash
ngrok http 5173
```

→ Скопируйте оба HTTPS URL!

## 5. Обновите конфигурацию

Откройте `client\.env` и замените:
```env
VITE_API_URL=https://abc123.ngrok.io/api
```
(Используйте ваш ngrok URL для **backend**)

**Перезапустите Frontend** - найдите окно с frontend, нажмите Ctrl+C и снова `npm run dev`

## 6. Настройте Mini App

1. [@BotFather](https://t.me/BotFather) → `/mybots`
2. Выберите бота → `Bot Settings` → `Menu Button`
3. `Configure Menu Button`
4. Название: `Открыть трекер`
5. URL: `https://def456.ngrok.io` (ваш ngrok URL для **frontend**)

## 7. Откройте в Telegram!

1. Найдите бота по username
2. Нажмите START
3. Нажмите кнопку меню или "Открыть трекер"

Готово! 🎉

---

**Нужна помощь?**
- Полная инструкция: [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md)
- Проблемы с ngrok: [NGROK_SETUP.md](./NGROK_SETUP.md)
